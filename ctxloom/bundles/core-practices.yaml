version: "1.0.0"
tags:
  - practices
  - workflow
  - quality
author: "ctxloom"
description: "Core development practices for code quality, documentation, and problem solving"

fragments:
  documentation:
    tags:
      - documentation
      - workflow
    content: |-
      # Documentation Guidelines

      ## README Management

      - **Read first** - Consult the README before taking action
      - **Update README** when adding new commands or features
      - README should always reflect the current state of the project

      ## Ask First Before Creating

      - Markdown files (*.md) - REFACTORING.md, architecture docs, etc.
      - Planning or strategy documents
      - Project tracking files
      - Meta-documentation

      **Exception:** README updates permitted when adding new features.

      ## Don't Document Progress in Code

      - No "reorganized code to..." comments
      - No "refactored X to Y" inline documentation
      - No changelog-style comments in code
      - Version control handles this - use commit messages instead

      ## Don't Include Change History in Files

      - No "Updated on..." or "Changed from..." annotations
      - No "Previously this was..." explanations
      - No revision history within data/content
      - History belongs in version control, not in the data itself

      ## Do

      - Suggest documentation needs first
      - Summarize findings in chat
      - Update existing documentation
      - Create code, tests, and configuration files
      - Write meaningful commit messages for progress tracking
    content_hash: sha256:2481f885e714c651a51c140c41e92dcff11e445283211847581b24343b319e8c
    distilled: |-
      # Documentation

      ## README
      - Read first before acting
      - Update when adding commands/features
      - Keep current with project state

      ## Ask Before Creating
      - *.md files (REFACTORING.md, arch docs)
      - Planning/strategy docs
      - Project tracking files
      - Meta-docs

      **Exception:** README updates for new features OK

      ## Don't
      - Add progress comments in code ("reorganized...", "refactored X to Y")
      - Include changelog-style comments
      - Add "Updated on...", "Changed from...", revision history in files
      - History belongs in VCS, not data

      ## Do
      - Suggest doc needs first
      - Summarize in chat
      - Update existing docs
      - Create code/tests/config
      - Use commit messages for tracking
    distilled_by: claude-code

  strings:
    tags:
      - code
      - strings
      - errors
    content: |-
      # String Handling

      ## No String-Based Flow Control

      Never use string approximations (`startswith`, `endswith`, `contains`, substring matching) for program flow control unless explicitly instructed. These are brittle and break silently when messages change.

      ```python
      # Wrong: brittle string matching
      if error.message.startswith("Invalid"):
          handle_validation_error()
      elif "not found" in error.message.lower():
          handle_not_found()

      # Right: use typed errors or error codes
      if isinstance(error, ValidationError):
          handle_validation_error()
      elif error.code == ErrorCode.NOT_FOUND:
          handle_not_found()
      ```

      ```go
      // Wrong
      if strings.Contains(err.Error(), "connection refused") {
          retry()
      }

      // Right
      if errors.Is(err, ErrConnectionRefused) {
          retry()
      }
      ```

      ## Error Messages as Constants

      All error messages must be defined as constants and reused in test assertions. No magic strings.

      ```python
      # constants.py
      ERR_USER_NOT_FOUND = "user not found"
      ERR_INVALID_EMAIL = "invalid email format"

      # service.py
      from .constants import ERR_USER_NOT_FOUND
      raise NotFoundError(ERR_USER_NOT_FOUND)

      # test_service.py
      from .constants import ERR_USER_NOT_FOUND
      assert str(exc.value) == ERR_USER_NOT_FOUND
      ```

      ```go
      // errors.go
      var (
          ErrUserNotFound = errors.New("user not found")
          ErrInvalidEmail = errors.New("invalid email format")
      )

      // service.go
      return ErrUserNotFound

      // service_test.go
      assert.ErrorIs(t, err, ErrUserNotFound)
      ```

      ## Why This Matters

      - String matching breaks silently when messages change
      - Typos in string comparisons cause subtle bugs
      - Constants enable IDE refactoring and find-all-references
      - Tests verify actual error handling, not string coincidences
      - Internationalization becomes possible
    content_hash: sha256:88b53b81c5c315b7879976858f39a1ed5eb71d390c47522fc448ca04d9e6037a
    distilled: |-
      # String Handling

      ## No String-Based Flow Control

      Never use string approximations (startswith, endswith, contains, substring) for flow control—brittle, breaks silently.

      ```python
      # Wrong
      if error.message.startswith("Invalid"): ...
      elif "not found" in error.message.lower(): ...

      # Right
      if isinstance(error, ValidationError): ...
      elif error.code == ErrorCode.NOT_FOUND: ...
      ```

      ```go
      // Wrong
      if strings.Contains(err.Error(), "connection refused") { retry() }

      // Right
      if errors.Is(err, ErrConnectionRefused) { retry() }
      ```

      ## Error Messages as Constants

      Define error messages as constants; reuse in tests. No magic strings.

      ```python
      # constants.py
      ERR_USER_NOT_FOUND = "user not found"

      # service.py
      raise NotFoundError(ERR_USER_NOT_FOUND)

      # test_service.py
      assert str(exc.value) == ERR_USER_NOT_FOUND
      ```

      ```go
      var ErrUserNotFound = errors.New("user not found")
      // service: return ErrUserNotFound
      // test: assert.ErrorIs(t, err, ErrUserNotFound)
      ```

      ## Why

      - String matching breaks silently on message changes
      - Typos cause subtle bugs
      - Constants enable IDE refactoring/find-refs
      - Tests verify error handling, not string coincidence
      - Enables i18n
    distilled_by: claude-code
