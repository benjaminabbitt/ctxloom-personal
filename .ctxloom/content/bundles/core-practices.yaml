name: core-practices
version: 1.0.0
tags:
- practices
- workflow
- quality
author: ctxloom
description: Core development practices for code quality, documentation, and problem solving
fragments:
    documentation:
        tags:
        - documentation
        - workflow
        content: |-
            # Documentation Guidelines

            ## Ask First Before Creating

            - Markdown files (*.md) - REFACTORING.md, architecture docs, etc.
            - Planning or strategy documents
            - Project tracking files
            - Meta-documentation

            **Exception:** README updates permitted when adding new features.

            ## Don't Document Progress in Code

            - No "reorganized code to..." or "refactored X to Y" comments
            - No changelog-style comments in code
            - Version control handles this - use commit messages instead

            ## Don't Include Change History in Files

            - No "Updated on...", "Changed from...", or "Previously this was..." annotations
            - No revision history within data/content
            - History belongs in version control, not in the data itself
        content_hash: sha256:9c4ca97e008558656b963f70d2940bc2780c8bb7ce7174fa12582a833bceab00
        distilled: |-
            # Documentation Guidelines

            Ask first before creating any *.md file, planning/strategy document, tracking file, or meta-documentation (exception: README updates when adding features). No progress notes in code ("refactored X to Y", changelog-style comments) and no change history in files ("Updated on...", "Previously this was..."); history belongs in version control and commit messages.
        distilled_by: claude-code
    strings:
        tags:
        - code
        - strings
        - errors
        content: |-
            # String Handling

            ## No String-Based Flow Control

            Never use string approximations (`startswith`, `endswith`, `contains`, substring matching) for program flow control unless explicitly instructed. Use typed errors or error codes instead.

            ```go
            // Wrong
            if strings.Contains(err.Error(), "connection refused") {
                retry()
            }

            // Right
            if errors.Is(err, ErrConnectionRefused) {
                retry()
            }
            ```

            ## Error Messages as Constants

            All error messages must be defined as constants and reused in test assertions. No magic strings.

            ```go
            // errors.go
            var ErrUserNotFound = errors.New("user not found")

            // service.go
            return ErrUserNotFound

            // service_test.go
            assert.ErrorIs(t, err, ErrUserNotFound)
            ```
        content_hash: sha256:5278e4fbf6ebf5b7bfe478c7ab3579c6297d3a1b7e73a49bce9b4ce88ac3cd3b
        distilled: |-
            # String Handling

            Never branch on string approximations of messages (startswith/contains/substring) unless explicitly instructed — use typed errors or error codes (`errors.Is(err, ErrConnectionRefused)`, not `strings.Contains(err.Error(), ...)`). Define all error messages as constants/sentinel errors and reuse them in test assertions — no magic strings.
        distilled_by: claude-code
